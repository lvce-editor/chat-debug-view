# Chat Debug View Worker

WebWorker for the chat debug view functionality in Lvce Editor.

## Contributing

```sh
git clone git@github.com:lvce-editor/chat-debug-view.git &&
cd chat-debug-view &&
npm ci &&
npm test
```
